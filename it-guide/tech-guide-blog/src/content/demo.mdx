# MDX with rehype-pretty-code

이 섹션은 `rehype-pretty-code`가 적용된 코드 블록을 보여줍니다.

```tsx
export default function Demo() {
  return <div className="text-neon-blue">Hello MDX!</div>;
}
```

## 하이라이팅, 라인 번호, 테마가 보입니다

단순한 MDX 내용입니다.
